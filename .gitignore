# Vector database — regenerated by running node ingest.js
vector-db/

# Source documents — may contain sensitive business data
documents/

# Session data — contains user conversations
sessions.json

# Logs — generated at runtime
logs/

# Environment variables — contains API keys
.env

# Dependencies — reinstalled via npm install
node_modules/